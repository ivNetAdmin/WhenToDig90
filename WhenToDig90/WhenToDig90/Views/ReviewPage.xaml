<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WhenToDig90.Views.ReviewPage">
  
   <ContentPage.ToolbarItems>
    <ToolbarItem Icon="{Binding CalendarIcon}" Command="{Binding CalendarNavigationCommand}" />
    <ToolbarItem Icon="{Binding JobIcon}" Command="{Binding JobNavigationCommand}" />
    <ToolbarItem Icon="{Binding ReviewIcon}" />
    <ToolbarItem Icon="{Binding PlantIcon}" Command="{Binding PlantNavigationCommand}" />
  </ContentPage.ToolbarItems>
  
  <ContentPage.Content>
    <Label Text="Review" VerticalOptions="Center" HorizontalOptions="Center" />
    <Label TextColor ="Red" Text="{Binding Message}" HorizontalOptions="Center" VerticalOptions="Center"/>	
  </ContentPage.Content>
</ContentPage>
